import assert from 'node:assert/strict';import test from 'node:test';import {fetchReport} from '../src/wb-api.js';
const response=(body,status=200)=>({ok:status===200,status,json:async()=>body});
test('постранично загружает отчёт без токена в URL',async()=>{const calls=[];const pages=[[{rrd_id:1},{rrd_id:2}],[{rrd_id:3}]];const rows=await fetchReport({token:'secret',dateFrom:'2026-08-01',dateTo:'2026-08-02',pageLimit:2,fetchImpl:async(url,opt)=>{calls.push([String(url),opt]);return response(pages.shift())}});assert.equal(rows.length,3);assert.match(calls[1][0],/rrdid=2/);assert.doesNotMatch(calls[0][0],/secret/);assert.equal(calls[0][1].headers.Authorization,'secret')});
test('отклоняет неверные даты и права',async()=>{await assert.rejects(fetchReport({token:'x',dateFrom:'bad',dateTo:'2026-01-01'}),/дата/);await assert.rejects(fetchReport({token:'x',dateFrom:'2026-01-02',dateTo:'2026-01-01'}),/позже/);await assert.rejects(fetchReport({token:'x',dateFrom:'2026-01-01',dateTo:'2026-01-01',fetchImpl:async()=>response({},403)}),/Статистика/)});
